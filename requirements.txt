# Core HTTP client
requests>=2.31.0

# LLM API (NVIDIA NIM — primary)
# Uses NVIDIA_API_KEY env var; compatible with any OpenAI-format API
# Set LLM_MODEL and LLM_API_ENDPOINT in .env to use a different provider

# If using Anthropic Claude directly (alternative to NVIDIA):
# anthropic>=0.20.0

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Data validation
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# CLI framework
typer>=0.9.0

# Rich terminal output
rich>=13.7.0

# Standard library extensions (no install needed):
# json, os, re, time, inspect, traceback, datetime — all built-in
